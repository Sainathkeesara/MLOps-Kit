# Added `README.md` to README Layout section

The Layout section of the README lists every notable file and directory in the repo. Except the one file the reader is looking at — `README.md` itself. Caught that after a quick scan of the structure.

Added it at the end of the list:

```
- **`README.md`** — Project overview, coverage table, and quick links
```

Put it at the end since it's not a directory and the existing list is directory-heavy. The description points at the three main sections someone would want to navigate to from the layout overview.
