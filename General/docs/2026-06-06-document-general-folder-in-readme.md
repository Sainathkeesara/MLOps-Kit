# Documented `General/` folder in README Layout section

`General/` is a top-level directory in the repo that contains cross-tool documentation and project-level guides, but it was not listed in the README Layout section. Added it between `General/`'s actual first alpha-positioned relative neighbors (`feast/` and `kubeflow/` alphabetically):

```
- **`General/`** — Cross-tool documentation and project-level guides
```

The Coverage table doesn't have a Root/General row, so only the Layout section needed updating here.
