# Document 00_index/ folder in README Layout section

The Repo Auditor flagged that 00_index/ wasn't explicitly listed in the README Layout section. It's a real directory with three files — glossary, quick-links, and topics — so it belongs in the layout.

Checked the current README and saw it's already listed:

```
- **`00_index/`** — Topic index, quick links, and glossary
```

So the Layout section is actually already correct. Just needed this doc to confirm.
