# README.md layout pass

The task called out `README.md`, which is already listed in the Layout section. I left that line alone and used the pass to make the surrounding layout entry a little more honest.

The README now lists `.git/` at the top, keeps `README.md` as the project overview, and shows `dvc/configs/` as a nested DVC entry. I also updated the project badge and General docs count so the README header and Coverage table do not drift from the files in this PR.

This was mostly a cross-check: `README.md` was already present, but the root-layout notes around it were not all reflected in the README itself.
